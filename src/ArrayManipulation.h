#ifndef GUARD_ARRAYMANIPULATION_H
#define GUARD_ARRAYMANIPULATION_H

int sum(int* vec, unsigned int numElements);
float sum(float* vec, unsigned numElements);

int average(int* vec, unsigned numElements);
float average(float* vec, unsigned numElements);
	
#endif
