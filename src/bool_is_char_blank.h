#ifndef GUARD_bool_is_char_blank_h
#define GUARD_bool_is_char_blank_h

//PERTINENT LIBRARIES

//FUNCTION PROTOTYPES
bool is_slash(char);
bool is_not_slash(char);

bool is_tab(char);
bool is_not_tab(char);

bool is_quote(char);
bool is_not_quote(char);

bool is_comma(char);
bool is_not_comma(char);

bool is_pound(char);
bool is_not_pound(char);

bool is_period(char);
bool is_not_period(char);

#endif